### 改进与修复
- 将 Gateway 的启动、重启与恢复决策集中到 Supervisor，应用端不再并行触发生命周期恢复，减轻「多端同时判障」导致的重启拉锯与健康状态抖动。
- 修复 Gateway 重启交接时的运行环境传递问题，并改进子进程异常退出后的恢复路径，提升长时间运行下的可恢复性。
- 收敛 Supervisor 的就绪等待与恢复节奏，优化失败场景下的提示文案；微调首页仪表盘展示。
- 移除已不再使用的旧版 Helper/池化相关代码路径，工程主线统一为用户态 Supervisor 托管，便于后续维护与演进。
